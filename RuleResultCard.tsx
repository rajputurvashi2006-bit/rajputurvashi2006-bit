import {AlertTriangle,CheckCircle2,GitBranch,Info,XCircle} from "lucide-react"; import type {RuleResult} from "../types"; import StatusBadge from "./StatusBadge";
export default function RuleResultCard({rules}:{rules:RuleResult[]}){return <div className="card overflow-hidden"><div className="flex items-center gap-2 border-b border-slate-200 px-5 py-4"><GitBranch size={18} className="text-navy-700"/><div><h3 className="text-sm font-semibold text-slate-800">Deterministic rule evaluation</h3><p className="text-[10px] text-slate-500">Rules operate on observed evidence; officer retains final authority.</p></div></div><div className="divide-y divide-slate-100">{rules.map(r=><div key={r.id} className="p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div className="flex items-start gap-3"><div className={`mt-0.5 rounded-md p-2 ${r.status==="PASS"?"bg-emerald-50 text-emerald-700":r.status==="FAIL"?"bg-red-50 text-red-700":"bg-amber-50 text-amber-700"}`}>{r.status==="PASS"?<CheckCircle2 size={17}/>:r.status==="FAIL"?<XCircle size={17}/>:<AlertTriangle size={17}/>}</div><div><div className="flex flex-wrap items-center gap-2"><h4 className="text-sm font-semibold text-slate-800">{r.title}</h4><code className="rounded bg-slate-100 px-1.5 py-0.5 text-[10px] text-slate-600">{r.ruleCode}</code></div><p className="mt-1 text-xs text-slate-500">{r.description}</p></div></div><StatusBadge status={r.status}/></div><div className="mt-4 grid gap-3 sm:grid-cols-3">{[["Observed",r.observedValue??"—"],["Expected",r.expectedValue??"—"],["Severity",r.severity]].map(([a,b])=><div key={a} className="rounded-md border border-slate-200 p-3"><p className="text-[10px] uppercase tracking-wide text-slate-400">{a}</p><p className="mt-1 text-xs font-semibold text-slate-700">{b}</p></div>)}</div><div className="mt-3 flex items-start gap-2 rounded-md bg-slate-50 p-3"><Info size={14} className="mt-0.5 text-slate-400"/><p className="text-xs leading-relaxed text-slate-600">{r.rationale}</p></div></div>)}</div></div>}