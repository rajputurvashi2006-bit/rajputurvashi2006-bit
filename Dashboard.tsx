import {AlertTriangle,ClipboardCheck,FileWarning,ShieldCheck} from "lucide-react"; import {Link} from "react-router-dom"; import {kpis,mockInspections} from "../data/mockData"; import KPICard from "../components/KPICard"; import InspectionTable from "../components/InspectionTable";
export default function Dashboard(){return <div className="space-y-6 p-6"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-widest text-navy-700">Inspection Control Centre</p><h1 className="mt-1 text-2xl font-bold text-slate-900">Good afternoon, Officer</h1><p className="mt-2 max-w-2xl text-sm text-slate-500">Review package evidence, verify deterministic checks, and maintain an auditable inspection record.</p></div><Link to="/inspections/new" className="btn-primary"><ClipboardCheck size={17}/>New Inspection</Link></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><KPICard kpi={kpis[0]} icon={<ClipboardCheck size={20}/>}/><KPICard kpi={kpis[1]} icon={<ShieldCheck size={20}/>}/><KPICard kpi={kpis[2]} icon={<AlertTriangle size={20}/>}/><KPICard kpi={kpis[3]} icon={<FileWarning size={20}/>}/></div><div className="grid gap-5 lg:grid-cols-3"><div className="card p-5 lg:col-span-2"><div className="flex items-start justify-between"><div><p className="section-title">Recent inspection activity</p><p className="mt-1 text-xs text-slate-500">Latest cases requiring attention or completed review.</p></div><Link to="/inspections" className="text-xs font-semibold text-navy-700">View all</Link></div><div className="mt-5"><InspectionTable inspections={mockInspections}/></div></div><div className="card p-5"><p className="section-title">Human-in-the-loop</p><div className="mt-5 space-y-3"><Link to="/review-queue" className="block rounded-md border border-amber-200 bg-amber-50 p-4"><p className="text-sm font-semibold text-amber-900">Manual decisions</p><p className="mt-1 text-xs text-amber-800">Conflicting evidence requires officer assessment.</p></Link><div className="rounded-md border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-700">Evidence-first principle</p><p className="mt-2 text-xs leading-relaxed text-slate-500">METROLENS provides observations and deterministic checks. Final legal determination remains with the authorized inspecting officer.</p></div></div></div></div></div>}