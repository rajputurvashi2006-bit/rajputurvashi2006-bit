import {CheckCircle2,ChevronDown,GitBranch,LockKeyhole,ShieldCheck} from "lucide-react"; import {useState} from "react"; import {ruleCatalog} from "../data/mockData";
export default function Rules(){const[expanded,setExpanded]=useState<string|null>(null);return <div className="space-y-6 p-6"><div><p className="text-xs font-semibold uppercase tracking-widest text-navy-700">Deterministic controls</p><h1 className="mt-1 text-2xl font-bold text-slate-900">Rules & Checks</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">Versioned inspection checks translate observed evidence into transparent pass, fail or manual-review outcomes.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="card p-5"><p className="text-xs font-semibold">Active rules</p><p className="mt-3 text-3xl font-bold">{ruleCatalog.length}</p></div><div className="card p-5"><p className="text-xs font-semibold">Version</p><p className="mt-3 text-3xl font-bold">v1.4</p></div><div className="card p-5"><p className="text-xs font-semibold">Execution</p><p className="mt-3 text-lg font-bold">Deterministic</p></div></div><div className="card overflow-hidden">{ruleCatalog.map(r=>{const open=expanded===r.code;return <div key={r.code} className="border-b border-slate-100"><button onClick={()=>setExpanded(open?null:r.code)} className="flex w-full items-center justify-between px-5 py-4 text-left hover:bg-slate-50"><div className="flex items-center gap-4"><div className="flex h-8 w-8 items-center justify-center rounded-md bg-navy-50 text-navy-700"><GitBranch size={15}/></div><div><div className="flex flex-wrap items-center gap-2"><code className="text-xs font-bold text-navy-700">{r.code}</code><span className="text-sm font-semibold text-slate-800">{r.title}</span></div><p className="mt-1 text-[10px] text-slate-400">{r.category} · {r.severity}</p></div></div><ChevronDown size={17} className={open?"rotate-180":""}/></button>{open&&<div className="border-t border-slate-100 bg-slate-50 px-5 py-4"><p className="text-xs leading-relaxed text-slate-600">{r.description}</p><div className="mt-3 flex items-center gap-2 text-xs text-slate-600"><CheckCircle2 size={14} className="text-emerald-600"/>Rule output describes evidence state, not an autonomous legal finding.</div></div>}</div>})}</div></div>}