import {AlertTriangle,Calculator,CheckCircle2,Ruler} from "lucide-react"; import type {Calibration} from "../types";
export default function CalibrationCard({calibration:c}:{calibration:Calibration}){if(!c.available)return <div className="card border-red-200"><div className="flex items-start gap-3 p-5"><div className="rounded-md bg-red-50 p-2 text-red-600"><AlertTriangle size={20}/></div><div><h3 className="text-sm font-semibold text-red-800">Calibration unavailable</h3><p className="mt-1 text-xs leading-relaxed text-red-700">No valid reference object was detected. Dimensional measurements must not be treated as calibrated evidence.</p></div></div></div>;return <div className="card overflow-hidden"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div className="flex items-center gap-2"><Ruler size={18} className="text-navy-700"/><div><h3 className="text-sm font-semibold text-slate-800">Calibration</h3><p className="text-[10px] text-slate-500">Measurement traceability</p></div></div><span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-semibold text-emerald-700"><CheckCircle2 size={12}/>VALID</span></div><div className="grid gap-4 p-5 sm:grid-cols-2">{[["Reference",c.referenceObject],["Reference length",`${c.referenceLengthMm} mm`],["Measured pixels",`${c.measuredPixels} px`],["Scale",`${c.scaleMmPerPixel.toFixed(4)} mm/px`]].map(([a,b])=><div key={a}><p className="text-[10px] uppercase tracking-wide text-slate-400">{a}</p><p className="mt-1 text-sm font-semibold text-slate-800">{b}</p></div>)}</div><div className="border-t border-slate-100 bg-slate-50 px-5 py-4"><div className="flex items-center gap-2"><Calculator size={15} className="text-navy-700"/><p className="text-xs font-semibold text-slate-700">Calibration math</p></div><code className="mt-2 block rounded-md border border-slate-200 bg-white p-3 text-xs text-slate-700">scale = reference length / measured pixels<br/>scale = {c.referenceLengthMm} / {c.measuredPixels}<br/>scale ≈ {c.scaleMmPerPixel.toFixed(4)} mm/px</code><p className="mt-2 text-[10px] text-slate-500">Estimated uncertainty: ±{c.uncertaintyMm} mm</p></div></div>}